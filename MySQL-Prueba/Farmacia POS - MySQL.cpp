#include <iostream>

//bool bQueryState;

using namespace std;
/*
void prueba() 
{

    MYSQL* Conexion;

    Conexion = mysql_init(0);
    Conexion = mysql_real_connect(Conexion, "localhost", "root", "root", "farmaciapos", 3306, NULL, 0);

    cout << "Valor de conexio: " << Conexion;

    if (Conexion)
    {
		cout << "Conexion exitosa" << endl;
        string usuario, passwd;

        cout << "Ingrese la fecha de fabricacion y vencimiento" << endl;
        cin >> usuario; 
        cin >> passwd;

        string insertar = "insert into tblprueba values ('" + usuario + "', '" + passwd + "')";

        const char *querry = insertar.c_str();
       // bQueryState = mysql_query(Conexion, querry);
	}
    else {
		cout << "Conexion fallida" << endl;
	}

}

*/